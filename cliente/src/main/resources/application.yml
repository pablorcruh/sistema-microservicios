server:
  port: 8000
spring:
  rabbitmq:
    host:
    port:
    username:
    password:
  application:
    name: cliente
  datasource:
    url: jdbc:postgresql://localhost:5432/clientes
    username: root
    password: password
    driver-class-name: org.postgresql.Driver

  jpa:
    open-in-view: false
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    hibernate:
      ddl-auto: create-drop
    properties:
      hibernate:
        show_sql: true

clientes:
  publish-order-events-job-cron: 1 * * * * *
  cliente-events-exchange: clientes-exchange
  new-cliente-queue: clientes